{
  "name": "fund-eye-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild --bundle --outfile=./dist/index.js --platform=node --target=node20 ./src/index.ts",
    "deploy": "npm run build && npx s deploy -y"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "hono-alibaba-cloud-fc3-adapter": "^1.0.2"
  },
  "devDependencies": {
    "@serverless-devs/s": "^3.0.0",
    "esbuild": "^0.20.0"
  }
}
